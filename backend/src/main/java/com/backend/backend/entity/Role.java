package com.backend.backend.entity;

/**
 * Enum representing the specific roles available in the application.
 * Using Enum ensures that only these predefined values can be stored in the database.
 */
public enum Role {
    STUDENT,
    ADMIN
}